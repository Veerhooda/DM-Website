# Cozy Nook Home Decor — D2C Assignment
Handmade cushions, planters & lamps storefront demo (Jaipur, Bapu Nagar).
Tech: vanilla HTML/CSS/JS — single `index.html`, no frameworks, no build.
Features: Shop filters, cart (`localStorage cozy_cart`), auth `Hi, Name` (`cozy_user`), checkout modal, theme toggle, mobile nav.
SEO: canonical `https://cozynookhomedecor.com/`, OG/Twitter, JSON-LD (Organization, WebSite+SearchAction, 8 Products, Breadcrumb, FAQ), `sitemap.xml`, `robots.txt`.
Pages: `/` (storefront), `/#shop` (shop anchor), `/case-study/` (redirect to `/`, `noindex`).
Run locally: `python3 -m http.server 8000` then open http://localhost:8000/
Deploy: any static host; set domain to `https://cozynookhomedecor.com/`.
Notes: Demo data only — `+91 98280 12345`, `hello@cozynookhomedecor.com`, Unsplash images.
License: For assignment/demo — not a real store.
